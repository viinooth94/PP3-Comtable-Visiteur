{# empty Twig template #}
{% block title %}
Accueil Comptable
{% endblock %}
{% extends "base.html.twig" %}
{% block nav %}
    <style>
            .imgnav{
                border-radius :25%;
            }
            #navi{
                overflow:hidden;
                position:fixed;
                top:0;
                width:100%;
                
            }
            
        </style>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark" id="navi" >
                   <a class="nav-link" href="AccueilComptable"> <img class="imgnav" src='img/logo.gif' width='50px'/></a>
                  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                  </button>
                  
                  <div class="collapse navbar-collapse" id="navbarNavDropdown">
                    <ul class="navbar-nav">
                       <li class="nav-item">
                            <a class="nav-link" href="{{ path('ValiderFicheFrais') }}">Valider fiche de frais</a>
                          </li>
                          <li class="nav-item">
                            <a class="nav-link" href="{{  path("SuivrePaiementFicheFrais") }}">Suivre paiment fiche de frais</a>
                        </li>
                      
                    </ul>
                  </div> 
                    <div style="float: right;  margin: 10px; color: #2fa4e0; font-family: 'URW Chancery L', cursive;  " ><h4><strong>{{ app.session.get('nom')}} {{ app.session.get('prenom')}}</strong></h4></div>
                  <div>
                     <a href="{{ path('DeconnexionComptable') }}" style="float: right" type="button" class="btn btn-outline-danger">Déconexion</a>                     
                  </div>
                  
                </nav>
                  
            

{% endblock %}